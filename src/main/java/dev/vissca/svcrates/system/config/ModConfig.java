package dev.vissca.svcrates.system.config;

/// Class that contains the values for the config, ModConfigManager turns this into a .json
public class ModConfig{
    public int crateChance = 6;
    public boolean displayCrateTooltip = true;
}